# ============================================================================
# BRIDE BUDDY ENVIRONMENT VARIABLES
# ============================================================================
#
# Copy this file to .env and fill in your actual values
# See ENVIRONMENT.md for detailed documentation on each variable
#
# IMPORTANT: Never commit .env to version control!
#
# Quick Start:
#   1. cp .env.example .env
#   2. Fill in all values below
#   3. Run: npm run build:config (to generate client config)
#   4. Run: npm run dev (to start development server)
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# ----------------------------------------------------------------------------
# Get these from: https://app.supabase.com > Your Project > Settings > API

# Your Supabase project URL
SUPABASE_URL=https://nluvnjydydotsrpluhey.supabase.co

# Public anon key (safe for client-side use, respects RLS policies)
# Used by frontend and backend API functions
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service role key (bypasses RLS - KEEP SECRET!)
# Only used in backend API functions, never expose to client
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ----------------------------------------------------------------------------
# ANTHROPIC API CONFIGURATION
# ----------------------------------------------------------------------------
# Get your key from: https://console.anthropic.com/ > API Keys
# Used for AI wedding planning chat powered by Claude

# Anthropic API key (KEEP SECRET - backend only)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ----------------------------------------------------------------------------
# STRIPE CONFIGURATION
# ----------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/ > Developers > API keys

# Stripe secret key (KEEP SECRET - backend only)
# Use test key (sk_test_...) for development
# Use live key (sk_live_...) for production
STRIPE_SECRET_KEY=your_stripe_secret_key_here

# Stripe webhook signing secret
# Get from: Stripe Dashboard > Developers > Webhooks > Add endpoint
# Endpoint URL should be: https://yourdomain.com/api/stripe-webhook
# Listen for: checkout.session.completed, customer.subscription.*
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# ----------------------------------------------------------------------------
# STRIPE PRICE IDs (REFERENCE)
# ----------------------------------------------------------------------------
# These price IDs are referenced in the code
# Update them in Stripe Dashboard > Products to match your subscription plans
#
# Current IDs:
#   Monthly VIP: price_1SHYkGDn8y3nIH6VnJNyAsE1
#   Until I Do (one-time): price_1SHYjrDn8y3nIH6VtE3aORiS
#
# To create new prices:
#   1. Go to Stripe Dashboard > Products
#   2. Create or edit a product
#   3. Add a new price
#   4. Copy the price ID and update your code
# ----------------------------------------------------------------------------
